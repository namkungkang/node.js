<%- include('nav.ejs') %>
  <div class="white-box">
    <% for (var i = 0; i < 글목록.length; i++){ %>
    <div class="list-box">
    <%- JSON.stringify(글목록[i]._id) %>
      <h4><a href="/detail/<%=글목록[i]._id%>"><%= 글목록[i].title %></a></h4>
    <p>글내용임</p>
  </div>
  <%}%>